name: love-compass
description: Dating app built with Next.js

services:
  web:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    env:
      DATABASE_URL: "file:./db/custom.db"
      DB_PROVIDER: "sqlite"
      NODE_ENV: "production"
    ports:
      - 3000:3000
    volumes:
      - db_data:/app/db
    healthcheck:
      path: /api/health
      port: 3000

volumes:
  db_data:
