name: love-compass

build:
  image: oven/bun:1

deploy:
  image: oven/bun:1

  readinessPath: /api/health

  scaling:
    min: 1
    max: 3

  health:
    httpPath: /api/health
    initialDelay: 30

env:
  NODE_ENV: production
