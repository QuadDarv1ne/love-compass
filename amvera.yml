---
meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  requirementsPath: package.json
  useCache: true
run:
  scriptName: package.json
  command: npm run build && npm run start
  persistenceMount: /data
  containerPort: 3000
